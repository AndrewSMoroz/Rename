<UserControl x:Class="Rename.Sections.Home.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rename.Sections.Home.Views"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="300">

    <!--<StackPanel Orientation="Vertical" Margin="10">
        <Button Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,0" Command="{Binding NavigateRenameCommand}">Rename</Button>
        <Button Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,10,0,0" Command="{Binding NavigateViewCommand}">View</Button>
        <Button Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,10,0,0" Command="{Binding NavigateSettingsCommand}">Settings</Button>
    </StackPanel>-->
    
</UserControl>
